/*1. Write a function that converts an array of strings into an array of numbers. Filter
out all non-numeric values.
Input: [&quot;1&quot;, &quot;21&quot;, undefined, &quot;42&quot;, &quot;1e+3&quot;, Infinity]
Output: [1, 21, 42, 1000]*/

/*function convertString (array) {
    newArray = [];
    var convertedElement;
    
for ( var i = 0; i < array.length; i++) {
    convertedElement = parseFloat(array[i]);

    if(isFinite(convertedElement)){
    newArray[newArray.length] = convertedElement;
}
    }
    
    return newArray;
}
    

var y = convertString(["1","21",undefined,"42","1e+3",Infinity]);
console.log(y);*/

/*2. Write a program to join all elements of the array into a string skipping elements
that are undefined, null, NaN or Infinity.
Input: [NaN, 0, 15, false, -22, " ", undefined, 47, null]
Output: “015false-2247”*/

function stringNumber (){
var newArray = "";
var convertedelement = "";
for (var i = 0; i< array.length; i++){
    if (parseInt(array[i])){
    newArray[newArray.length] = convertedelement;
}
if (parseInt(array[i])) && (array[i]) === Boolean
}
}
